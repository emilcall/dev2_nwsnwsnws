
<h1><%= title %></h1>

<% if( news.length === 0 ) { %>
  <p>Er is geen nieuws beschikbaar</p>
<% } else { %>
    <% news.forEach((item) => { %>
        <h2><%= item.title %></h2>
        <p><%= item.content %></p>
    <% }) %>
<% } %>